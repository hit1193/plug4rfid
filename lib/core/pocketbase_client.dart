import 'package:pocketbase/pocketbase.dart';

/// ---------------------------------------------------------------------------
/// [전역 포켓베이스 클라이언트 (DataModule 역할)]
/// 앱 전체에서 단 하나의 동일한 객체(인스턴스)를 공유하여
/// 로그인 상태(관리자 인증 토큰)를 모든 프로바이더가 공통으로 유지하게 합니다.
/// ---------------------------------------------------------------------------
final PocketBase pb = PocketBase('http://127.0.0.1:8090');